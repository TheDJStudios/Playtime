package com.generated.playtime;

import com.generated.playtime.event.ModEvents;
import net.minecraftforge.fml.common.Mod;
import net.minecraftforge.fml.event.lifecycle.FMLClientSetupEvent;
import net.minecraftforge.fml.event.lifecycle.FMLCommonSetupEvent;
import net.minecraftforge.fml.javafmlmod.FMLJavaModLoadingContext;
import net.minecraftforge.eventbus.api.IEventBus;
import net.minecraft.client.renderer.ItemBlockRenderTypes;
import net.minecraft.client.renderer.RenderType;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

/**
 * Generated by fabricpy — Playtime v1.0
 */
@Mod("playtime")
public class Playtime {
    public static final String MOD_ID = "playtime";
    public static final Logger LOGGER = LogManager.getLogger(MOD_ID);

    public Playtime() {
        IEventBus bus = FMLJavaModLoadingContext.get().getModEventBus();

        ModBlocks.BLOCKS.register(bus);
        ModItems.ITEMS.register(bus);
        
        ModCreativeTabs.TABS.register(bus);
        
        
        
        bus.addListener(ModEvents::onCommonSetup);
        bus.addListener(ModBlocks::addCreative);
        bus.addListener(ModItems::addCreative);
        
        bus.addListener(this::onClientSetup);

        // Register command events on Forge's main bus
        // No commands
        net.minecraftforge.common.MinecraftForge.EVENT_BUS.register(ModEvents.class);
    }

    public void onClientSetup(FMLClientSetupEvent event) {
        event.enqueueWork(() -> {
            ItemBlockRenderTypes.setRenderLayer(ModBlocks.HAND_SCANNER.get(), RenderType.solid());
            ItemBlockRenderTypes.setRenderLayer(ModBlocks.HAND_SCANNER_RED_RIGHT.get(), RenderType.solid());
            ItemBlockRenderTypes.setRenderLayer(ModBlocks.CRITTER_PLUSH_HOPPY.get(), RenderType.solid());
        });
    }
}
